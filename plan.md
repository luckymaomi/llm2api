# 动态模型发现与上游 Key 管理闭环重建

## 需求文档

- 用户与场景：管理员维护合法取得的多把上游 API Key；成员通过套餐和自己的 API 密钥调用指定资源池内的指定模型。
- 要解决的问题：当前模型由代码静态目录、资源池表单和 Key 表单重复声明，真实 `/models` 响应被丢弃；因此模型显示不全、Key 资格不真实、批量测试仍发送 `hi`，测试界面和资源池表格也缺乏稳定对齐。
- 当前阶段范围：重建动态模型发现、Key 级模型资格、资源池模型并集、轻量/深度测试、资源池与 Key 管理界面，并同步事实文档和定向测试。
- 可验收完成标准：资源池只选择 Provider；添加或更换 Key 时由服务端探测并保存真实模型 ID；资源池展示所有活动 Key 的模型并集；同一模型只调度支持它的同池 Key；默认测试不发送生成请求；深度测试明确提示可能消耗 Token；失败不伪造或清空上次成功事实。

## 当前事实

- 已确认的源码、数据、配置、测试和运行事实：`providers.Catalog -> registry.SyncCatalog -> models` 在启动时写静态模型；资源池和 Key 接口接受前端模型绑定；Provider `Probe` 已能构造 `GET /models`，但 `ValidateProbe` 只验证 `data` 数组而丢弃模型 ID；现有 Key 测试通过 Chat 发送 `hi`。
- 已核验的外部合同：本仓库 OpenAI-compatible、Agnes、智谱适配器声明支持模型列表探测；模型列表是读操作且不发送生成消息。Provider 只拥有 wire、认证、错误分类与可保守确认的协议能力，不拥有某把 Key 当前可见的模型集合。
- 参考实现：本机 `repository/llmapi` 是无 Git HEAD 的 Sub2API 本地快照，许可证为 LGPL-3.0；参考 `AccountTestModal.vue` 的单列任务流、定宽结果区和明确 footer，以及 `upstream_models.go` 的实时列表解析与安全错误分类。只借鉴机制，不复制源码；拒绝它仍存在的静态默认模型回退、渐变卡片和复杂终端样式。
- 未知项及其验证方式：不同 Provider 返回的模型元数据完整度不一致，通过适配器 wire fixture 验证；无法从响应权威确认的模型级工具、上下文或输出上限保持未知，不编造数值。
- 工作区已有改动与保护边界：保留当前成本/额度简化、同池故障转移、共享健康、调用示例和样式调整；保留 owner 的 `.gitignore`、`ref/go.mod` 删除与 `test.md`；删除本轮误加的智谱静态模型列表；不 commit、push、部署或重置数据库。

## 失败证据

- 失败仍存在时可观察的产品结果：智谱创建资源池只显示静态 `GLM-5.2`；Agnes 三把可用 Key 的生成测试出现与价格无关的错误；资源池与 Key 的真实模型集合不一致；“测试全部 Key”会产生生成请求；探测弹窗与表格列不齐。
- 可重复的测试、命令或日志证据：`test.md` 的资源池截图；源码可见 `SyncCatalog` 写模型、前端提交 `modelIds/model_bindings`、`ValidateProbe` 丢弃 `data[].id`、`ProbeCredential` 固定为 `generation`。

## 最终目标

- 当前阶段完成后的生产级终局：Provider 是代码拥有的连接能力；上游 Key 最新成功的模型发现快照是 Key 资格 owner；资源池当前模型集合是活动 Key 快照的并集；套餐和下游 API 密钥只引用稳定的池内模型路由；调度仍严格停留在选定资源池内。
- 必须保持的不变量：不跨 Provider/资源池；不把探测失败当成模型被删除；不由前端声明 Key 支持模型；不把“能列模型”冒充“生成已验证”；不记录或回显 secret；不因动态发现引入静态回退、双读或兼容壳。

## 不做范围

- 当前阶段明确不实现的能力：模型价格/成本、在线采购金额、公共注册、跨池回退、自动换模型、移动端布局、周期后台爬取、模型别名市场。
- 明确不建立的兼容、过渡或双轨路径：不保留资源池创建时模型复选框、Key 手工模型绑定、静态模型投影或探测失败后的静态目录兜底；首次生产前直接压平基线。

## 设计

### 事实 Owner

- 公共协议：请求中的模型名解析为套餐版本固定的 `资源池 + 模型` 路由；模型名不改变资源池。
- 状态与持久化：`models` 保存 Provider 内稳定模型身份；`credential_models` 保存某把 Key 最新成功发现的模型资格；`resource_pool_models` 只保存已发现过且可被套餐历史引用的池内路由身份，当前可用并集由活动 Key 动态派生。
- 外部接线：Provider Adapter 构造有界 `GET /models`、解析和规范化 ID、过滤无法由 Chat 协议承载的条目，并分类认证、限流、超时、畸形或过大响应。
- 错误与恢复：创建/更换 secret 先发现后事务提交；刷新失败保留上次成功快照并记录失败；成功的空集合使该 Key 暂无可调度模型；并发刷新用期望版本拒绝过期提交。
- 展示与可观测性：列表展示最近发现状态、延迟、时间和模型数量；模型 ID 可完整查看；错误只暴露安全分类和请求 ID；轻量探测与深度生成测试分开命名。

### 数据与控制流

- 创建资源池：管理员选择 Provider 和名称，提交后得到空池；上下文引导下一步添加上游 API Key。
- 添加 Key：服务端以未持久化 secret 调用 `/models`，校验并规范化 ID；单事务加密保存 Key、upsert 模型身份、替换 Key 绑定、登记池内路由和成功探测事实；批量导入逐行返回成功或安全错误。
- 刷新 Key：解密当前 secret 后探测；成功时原子替换最新绑定，失败时只更新失败事实；更换 secret 只有新 secret 探测成功才原子替换。
- 调度：套餐解析固定池与模型；候选查询只返回同池、活动、健康且在 `credential_models` 中支持该模型的 Key；现有 priority、weight、容量、冷却、熔断和有界重试继续生效。
- 深度测试：管理员从该 Key 已发现且可路由的模型中选择一个，显式发起最小生成请求；结果区稳定展示状态、延迟、Token 和请求 ID。

### 安全与数据

- `/models` 请求继续经过受信 Provider 基础 URL 与 URL 校验；限制超时、重定向、响应字节数、模型数量和单个 ID 长度。
- secret 只在内存短暂使用并加密持久化；日志、审计、错误、批量结果和浏览器响应均不包含 secret 或上游原始 body。
- 仅活动管理员可创建、刷新、替换和深度测试上游 Key；成员只读取套餐投影后的可用模型。

### 关键取舍

- 采用方案及证据：Key 级最新成功快照能准确表达“每个小水池支持哪些模型”，池级并集表达“大水池”；失败保留快照避免瞬时网络故障把服务清空；轻量探测不消耗生成额度。
- 被拒绝方案及原因：静态目录会过期且不能表达 Key 差异；前端复选框会伪造上游资格；只看 HTTP 200 不能确认响应是合法模型列表；每次都发 `hi` 会产生不必要费用且把连通性与生成能力混为一谈；跨池回退破坏套餐合同。

## 生产级切片

- [ ] 切片 1：Provider 模型发现合同、错误分类、资源限制与 wire 测试。
- [ ] 切片 2：动态模型身份、Key 快照、池内并集和并发安全事务。
- [ ] 切片 3：资源池/Key 公共管理 API 和批量导入改为服务端发现。
- [ ] 切片 4：资源池、Key、轻量探测和深度测试桌面任务流重建。
- [ ] 切片 5：调度资格、文档、生成物、定向测试和交付检查。

## 实施任务

- [x] 完成全局核心语义调查
- [x] 固化失败证据与关键不变量
- [ ] 删除静态模型投影和错误的智谱静态目录
- [ ] 实现模型发现解析器与 Key 级快照 owner
- [ ] 重建资源池、Key 与批量导入 API 合同
- [ ] 重建轻量探测、深度测试与桌面 UI
- [ ] 对齐调度、套餐、下游模型列表和新手引导
- [ ] 同步 schema 生成物、事实文档与验证入口
- [ ] 运行一分钟内定向测试、构建和静态检查
- [ ] 检查差异、敏感信息和交付边界

## 恶劣路径矩阵

| 边界 | 接受/提交事实 | 失败状态 | 恢复 owner | 重放与幂等 | 验证证据 |
| --- | --- | --- | --- | --- | --- |
| 重复创建/刷新 | 同一幂等键或期望版本只提交一次 | conflict | registry/store | 指纹相同复用，过期版本拒绝 | repository/service test |
| 客户端断连/取消 | 上游发现可取消；已进入事务独立收口 | cancelled/unknown | service/store | GET 可有界重试，事务不半提交 | cancellation test |
| 上游超时、429、5xx | 不替换成功快照 | safe error kind | adapter/service | GET 可重试；生成测试按现有策略 | adapter fixture |
| 畸形/过大模型响应 | 不接受模型事实 | invalid/too_large | adapter | 不持久化原始 body | parser test |
| 并发刷新/更换 secret | 仅匹配期望版本的提交生效 | conflict | store | 乐观并发 | repository test |
| 进程强杀/主机重启 | 事务前无新事实，事务后完整可恢复 | none/last success | PostgreSQL | 单事务原子性 | integration test |
| PostgreSQL/Valkey 故障 | PostgreSQL 失败关闭；模型发现不依赖 Valkey | unavailable | store/coordinator | 不用内存冒充持久事实 | integration test |
| Key 被停用/模型消失 | 当前并集和候选实时排除；历史路由保留 | temporarily unavailable | query owner | 成功快照替换幂等 | routing test |

## 验证计划

- 定向检查：Provider 探测解析、registry/store 动态绑定、同池候选过滤、控制 API 合同、前端 typecheck/build 中预计一分钟内的目标。
- 完整验证：由 owner 后续运行 `python .\start_test.py full`；本轮不擅自运行长档。
- 竞态/并发验证：定向 repository 测试覆盖并发刷新和失败保留；既有 `-race` 长验证交给 owner 统一入口。
- 目标平台构建：Windows PowerShell 下使用 `npm.cmd`；Go/前端定向构建若预计不超过一分钟则执行。
- 隔离的真实 Provider 验收：由 owner 明确授权后，用测试 Key 验证 Agnes/智谱 `/models` 与可选深度生成；本轮不读取真实 Key。
- 安全与敏感信息检查：检查 diff、日志、错误和测试夹具不包含 secret、上游 body 或个人数据。

## 收口

- 完成事实：执行中持续更新。
- 实际命令与结果：执行中持续更新。
- 未验证项：真实 Provider、真实浏览器、full/capacity/deployment 长档由 owner 授权运行。
- 剩余风险：部分上游模型列表不提供模型级能力元数据，只能按 Adapter 保守公共合同路由并将未知值留空。
- commit/push/部署状态：未授权，不执行。
